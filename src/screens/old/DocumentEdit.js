import React , { Component } from 'react';
import { ScrollView, View, Text, TextInput } from 'react-native';
import { Constants } from 'expo';

class PushDocumentEditSCreen extends Component{
    constructor(props){
        super(props)
        this.state={
            repoID:'',
        }
    }

    render(){
        return(
            <ScrollView>

            </ScrollView>
        );
    }
}

export default (PushDocumentEditSCreen);
